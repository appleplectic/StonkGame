from pynput import keyboard

if __name__ == "__main__":
    class Stock():
        def __init__(self, name, symbol, per, cap):
            self.name = name
            self.symbol = symbol
            self.per = per
            self.cap = cap


    print("Butthole Milkshake")
